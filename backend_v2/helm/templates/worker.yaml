apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-worker
spec:
  replicas: {{ .Values.replicas.worker }}
  selector:
    matchLabels: {app: {{ .Release.Name }}-worker}
  template:
    metadata:
      labels: {app: {{ .Release.Name }}-worker}
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        fsGroup: 10001
        seccompProfile: {type: RuntimeDefault}
      containers:
        - name: worker
          image: {{ include "bda.backendImage" . | quote }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command: ["celery", "-A", "backend_v2.app.compute.tasks.celery_app", "worker", "-Q", "dispatch,poll,collect,maintenance", "--loglevel=info", "--concurrency={{ .Values.config.workerConcurrency }}"]
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            capabilities: {drop: ["ALL"]}
          envFrom:
            - configMapRef: {name: {{ .Release.Name }}-config}
            - secretRef: {name: {{ .Values.existingSecret }}}
          env:
            - name: BDA_V2_DATABASE_URL
              valueFrom: {secretKeyRef: {name: {{ .Values.workerSecret }}, key: BDA_V2_DATABASE_URL}}
            - {name: BDA_V2_DATABASE_POOL_SIZE, value: "2"}
            - {name: BDA_V2_DATABASE_MAX_OVERFLOW, value: "0"}
            - {name: BDA_V2_WORKER_QUEUES, value: "dispatch,poll,collect,maintenance"}
          resources: {{- toYaml .Values.resources.worker | nindent 12 }}
          volumeMounts:
            - {name: tmp, mountPath: /tmp}
            {{- include "bda.lsfVolumeMount" . | nindent 12 }}
      volumes:
        - {name: tmp, emptyDir: {}}
        {{- include "bda.lsfVolume" . | nindent 8 }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-research-worker
spec:
  replicas: {{ .Values.replicas.researchWorker }}
  selector:
    matchLabels: {app: {{ .Release.Name }}-research-worker}
  template:
    metadata:
      labels: {app: {{ .Release.Name }}-research-worker}
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        fsGroup: 10001
        seccompProfile: {type: RuntimeDefault}
      containers:
        - name: research-worker
          image: {{ include "bda.backendImage" . | quote }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command: ["celery", "-A", "backend_v2.app.compute.tasks.celery_app", "worker", "-Q", "research", "--loglevel=info", "--concurrency={{ .Values.config.workerConcurrency }}"]
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            capabilities: {drop: ["ALL"]}
          envFrom:
            - configMapRef: {name: {{ .Release.Name }}-config}
            - secretRef: {name: {{ .Values.existingSecret }}}
          env:
            - name: BDA_V2_DATABASE_URL
              valueFrom: {secretKeyRef: {name: {{ .Values.workerSecret }}, key: BDA_V2_DATABASE_URL}}
            - {name: BDA_V2_DATABASE_POOL_SIZE, value: "2"}
            - {name: BDA_V2_DATABASE_MAX_OVERFLOW, value: "0"}
            - {name: BDA_V2_WORKER_QUEUES, value: "research"}
          resources: {{- toYaml .Values.resources.researchWorker | nindent 12 }}
          volumeMounts:
            - {name: tmp, mountPath: /tmp}
            {{- include "bda.byokVolumeMount" . | nindent 12 }}
      volumes:
        - {name: tmp, emptyDir: {}}
        {{- include "bda.byokVolume" . | nindent 8 }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-copilot-worker
spec:
  replicas: {{ .Values.replicas.copilotWorker }}
  selector:
    matchLabels: {app: {{ .Release.Name }}-copilot-worker}
  template:
    metadata:
      labels: {app: {{ .Release.Name }}-copilot-worker}
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        fsGroup: 10001
        seccompProfile: {type: RuntimeDefault}
      containers:
        - name: copilot-worker
          image: {{ include "bda.backendImage" . | quote }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          command: ["celery", "-A", "backend_v2.app.compute.tasks.celery_app", "worker", "-Q", "copilot", "--loglevel=info", "--concurrency={{ .Values.config.workerConcurrency }}"]
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            capabilities: {drop: ["ALL"]}
          envFrom:
            - configMapRef: {name: {{ .Release.Name }}-config}
            - secretRef: {name: {{ .Values.existingSecret }}}
          env:
            - name: BDA_V2_DATABASE_URL
              valueFrom: {secretKeyRef: {name: {{ .Values.workerSecret }}, key: BDA_V2_DATABASE_URL}}
            - {name: BDA_V2_DATABASE_POOL_SIZE, value: "2"}
            - {name: BDA_V2_DATABASE_MAX_OVERFLOW, value: "0"}
            - {name: BDA_V2_WORKER_QUEUES, value: "copilot"}
          resources: {{- toYaml .Values.resources.copilotWorker | nindent 12 }}
          volumeMounts:
            - {name: tmp, mountPath: /tmp}
            {{- include "bda.byokVolumeMount" . | nindent 12 }}
      volumes:
        - {name: tmp, emptyDir: {}}
        {{- include "bda.byokVolume" . | nindent 8 }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-beat
spec:
  replicas: 1
  selector:
    matchLabels: {app: {{ .Release.Name }}-beat}
  template:
    metadata:
      labels: {app: {{ .Release.Name }}-beat}
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        fsGroup: 10001
        seccompProfile: {type: RuntimeDefault}
      containers:
        - name: beat
          image: {{ include "bda.backendImage" . | quote }}
          command: ["celery", "-A", "backend_v2.app.compute.tasks.celery_app", "beat", "--loglevel=info", "--schedule=/tmp/celerybeat-schedule"]
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            capabilities: {drop: ["ALL"]}
          envFrom:
            - configMapRef: {name: {{ .Release.Name }}-config}
            - secretRef: {name: {{ .Values.existingSecret }}}
          env:
            - name: BDA_V2_DATABASE_URL
              valueFrom: {secretKeyRef: {name: {{ .Values.workerSecret }}, key: BDA_V2_DATABASE_URL}}
            - {name: BDA_V2_DATABASE_POOL_SIZE, value: "1"}
            - {name: BDA_V2_DATABASE_MAX_OVERFLOW, value: "0"}
          volumeMounts: [{name: tmp, mountPath: /tmp}]
      volumes: [{name: tmp, emptyDir: {}}]
